# 09. 빌드 및 배포

## 스크립트

```bash
npm run dev          # 개발 서버 (localhost:5174)
npm run build        # 프로덕션 빌드 → dist/
npm run preview      # 빌드 결과 미리보기
npm run lint         # ESLint 검사
npm run generate-og  # OG 이미지 재생성 (sharp)
npm run deploy       # 빌드 + gh-pages 배포 (predeploy → deploy)
```

## 빌드 결과 (최신)

### 출력 구조
- 출력 디렉토리: `dist/`
- 에셋 디렉토리: `dist/assets/`
- 빌드 시간: ~7초

### 파일 크기

| 파일 | 크기 | gzip |
|------|------|------|
| **CSS (전체)** | 52.74 KB | 9.05 KB |
| **JS (메인 번들)** | 438.09 KB | 129.45 KB |
| learning.js (학습 콘텐츠) | 29.16 KB | 11.98 KB |
| tools.js (도구 데이터) | 15.73 KB | 5.30 KB |
| Practice.js (퀴즈) | 13.67 KB | 5.49 KB |
| Glossary.js (용어사전) | 10.69 KB | 4.41 KB |
| Home.js | 8.72 KB | 2.22 KB |
| ToolsOverview.js | 6.58 KB | 1.77 KB |
| curriculum.js | 6.20 KB | 2.63 KB |
| ToolDetail.js | 5.86 KB | 1.63 KB |
| About.js | 5.36 KB | 1.90 KB |
| CourseDetail.js | 5.24 KB | 1.56 KB |
| Enrollment.js | 4.60 KB | 1.59 KB |
| LearningPage.js | 4.46 KB | 1.69 KB |
| Request.js | 2.81 KB | 1.02 KB |
| Login.js | 2.62 KB | 1.31 KB |
| Community.js | 2.31 KB | 0.84 KB |
| SEOHead.js | 1.10 KB | 0.58 KB |
| NotFound.js | 0.68 KB | 0.43 KB |
| useAOS.js | 0.33 KB | 0.27 KB |

## 배포: GitHub Pages (gh-pages)

### 방법
```bash
npm run deploy
# → npm run predeploy (build) → gh-pages -d dist
```

1. `npm run build`로 dist/ 생성
2. `gh-pages` 패키지가 dist/ 폴더를 `gh-pages` 브랜치에 배포
3. GitHub 리포지토리 Settings > Pages에서 gh-pages 브랜치 선택

### 커스텀 도메인
- `public/CNAME`: `presentation.dreamitbiz.com`
- DNS: CNAME 레코드 → `aebonlee.github.io`

### SPA 라우팅 지원
- `public/404.html`: 경로를 쿼리스트링으로 인코딩하여 `index.html`로 리다이렉트
- `index.html`: `sessionStorage.redirect`로 원래 경로 복원

## Vite 설정 (vite.config.js)
```javascript
{
  plugins: [react()],
  base: '/',
  build: {
    outDir: 'dist',
    assetsDir: 'assets',
    emptyOutDir: true
  },
  server: {
    host: true,
    port: 5174
  }
}
```

## 파비콘
- `public/favicon.svg`: SVG 파비콘 (블루 그래디언트 배경 + P 로고)
- `public/favicon.ico`: ICO 파비콘 (32×32, 둥근 모서리)
- `index.html`에 `<link>` 태그 3개 등록 (SVG, ICO, apple-touch-icon)
