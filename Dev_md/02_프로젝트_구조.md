# 프로젝트 구조

```
D:\dreamit-web\presentation\
├── public/
│   ├── 404.html                    # GitHub Pages SPA 리다이렉트
│   ├── CNAME                       # 커스텀 도메인 (presentation.dreamitbiz.com)
│   └── og-image.png                # OG 이미지 (1200x630, sharp로 생성)
│
├── scripts/
│   └── generate-og-image.js        # OG 이미지 생성 스크립트 (SVG→PNG)
│
├── src/
│   ├── main.jsx                    # React 엔트리 포인트
│   ├── App.jsx                     # 라우터 설정 (React Router v7)
│   ├── index.css                   # 글로벌 CSS import 매니페스트
│   │
│   ├── styles/                     # CSS 디자인 시스템
│   │   ├── base.css                # 디자인 토큰, CSS 변수, 리셋, 유틸리티
│   │   ├── dark-mode.css           # 다크 모드 오버라이드
│   │   ├── navbar.css              # 네비게이션 바
│   │   ├── hero.css                # 히어로 섹션
│   │   ├── footer.css              # 푸터
│   │   ├── curriculum.css          # 커리큘럼/과정 페이지
│   │   ├── tools.css               # 도구 페이지 (카드, 상세, 가격표, 비교표)
│   │   ├── auth.css                # 로그인/인증 + 토스트 알림
│   │   ├── pages.css               # 공통 페이지 (특징, CTA, 소개, 대상, 404, 로딩)
│   │   ├── animations.css          # AOS 애니메이션 (fade-up/down/left/right, zoom-in)
│   │   └── responsive.css          # 반응형 브레이크포인트
│   │
│   ├── contexts/                   # React Context (전역 상태)
│   │   ├── ThemeContext.jsx        # 테마(light/dark/auto) + 컬러(5종) 관리
│   │   ├── AuthContext.jsx         # Supabase 인증 상태 관리
│   │   └── ToastContext.jsx        # 토스트 알림 관리
│   │
│   ├── hooks/                      # 커스텀 훅
│   │   └── useAOS.js              # Intersection Observer 스크롤 애니메이션
│   │
│   ├── utils/                      # 유틸리티
│   │   ├── supabase.js            # Supabase 클라이언트 (PKCE 인증)
│   │   └── auth.js                # 인증 헬퍼 (Google/카카오 OAuth, 로그아웃)
│   │
│   ├── data/                       # 정적 데이터
│   │   ├── curriculum.js          # 5개 커리큘럼 데이터 (모듈, 토픽, 특징)
│   │   └── tools.js               # 6개 도구 데이터 (가격, 기능, 팁, 비교표)
│   │
│   ├── components/                 # 재사용 컴포넌트
│   │   ├── SEOHead.jsx            # OG/Twitter/SEO 메타태그 관리
│   │   └── layout/
│   │       ├── Navbar.jsx         # 글래스모피즘 네비게이션 바
│   │       └── Footer.jsx         # 푸터 (패밀리 사이트, 연락처)
│   │
│   ├── layouts/                    # 레이아웃
│   │   └── PublicLayout.jsx       # 공개 페이지 레이아웃 (Navbar + Outlet + Footer)
│   │
│   └── pages/                      # 라우트 페이지
│       ├── Home.jsx               # 홈 (히어로, 커리큘럼, 도구, 특징, 대상, CTA)
│       ├── About.jsx              # 프로그램 소개 (특징, 로드맵)
│       ├── Login.jsx              # 로그인 (Google/카카오 OAuth)
│       ├── NotFound.jsx           # 404 페이지
│       ├── courses/
│       │   └── CourseDetail.jsx   # 커리큘럼 상세 (동적 라우트: /curriculum/:slug)
│       └── tools/
│           ├── ToolsOverview.jsx  # 도구 비교 (카드, 비교표, 상황별 추천)
│           └── ToolDetail.jsx     # 도구 상세 (가격, 기능, 장단점, 팁)
│
├── Dev_md/                         # 개발 문서
├── dist/                           # 빌드 결과물
├── index.html                      # Vite 엔트리 HTML
├── package.json                    # 의존성 및 스크립트
├── vite.config.js                  # Vite 설정
├── eslint.config.js                # ESLint 설정
├── .env                            # 환경 변수 (Supabase)
└── .gitignore                      # Git 무시 파일
```

## 파일 수
- **설정 파일**: 6개
- **CSS 파일**: 11개
- **컨텍스트**: 3개
- **유틸리티**: 2개
- **데이터**: 2개
- **컴포넌트**: 3개
- **페이지**: 7개
- **스크립트**: 1개
- **총 소스 파일**: 40개
